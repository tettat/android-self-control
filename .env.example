# Default relay server URL (used as default in app settings)
# Leave empty if you don't have one — users can fill it in the app
DEFAULT_RELAY_URL=

# Extra domains that need cleartext (HTTP) traffic permission
# Comma-separated, e.g.: 192.168.1.100,my-server.local
CLEARTEXT_DOMAINS=
